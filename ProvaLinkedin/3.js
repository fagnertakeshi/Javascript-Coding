/*3 - Which statement creates a new function called discountPrice?

a) ( ) let function = discountPrice(price) {
return price * 0.85;
}
b) ( ) let discountPrice(price) {
return price *0.85;
}
c) ( ) {
discountPrice:function(price) {
return price* 0.85;
}
}
d)(X) let discountPrice = function(price) {
return price* 0.85;
};*/

let  discountPrice = function(price) {
    return price * 0.85;
}

